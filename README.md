# ffmpeg-lgpl-docker
An LGPL build of FFmpeg packaged as a Docker Image

## Quick Start

To build and test the LGPL-compliant FFmpeg Docker image:

```powershell
cd container.tests
./test.ps1
```

For detailed build instructions and usage, see [BUILD.md](BUILD.md).
